<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">

<div class="container">
  <div class="welcome-message jumbotron mt-5">
    <h1 class="display-4">ようこそ！Old_shrineへ！</h1>
    <p class="lead">このサイトは廃神社を閲覧できるSNSです。</p>
    <hr class="my-4">
    <p>アカウントを作成せずに写真を閲覧できますが、いいねやコメントをするにはアカウントの作成が必要です。</p>
  </div>

  <div class="timeline">
    <div class="d-flex justify-content-between align-items-center">
      <h2 class="mb-3">みんなの最新の投稿</h2>
      <%= link_to "新規投稿", new_photo_path, class: "btn btn-primary" %>
    </div>
    <ul class="list-unstyled">
      <% if @timeline_photos %>
        <% @timeline_photos.each do |photo| %>
          <li class="media my-4">
            <% if photo.image.attached? %>
              <% variant = photo.image.variant(resize: "300x300") %>
              <%= image_tag(url_for(variant), class: "mr-3") %>
            <% end %>
            <div class="media-body">
              <h5 class="mt-0 mb-1"><%= photo.title %></h5>
              <!-- 他の写真の情報を表示するコードを追加 -->
            </div>
          </li>
        <% end %>
      <% else %>
        <li>タイムライン上の写真はありません。</li>
      <% end %>
    </ul>
  </div>
</div>

<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
